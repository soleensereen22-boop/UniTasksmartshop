{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Open HTML file directly",
            "file": "${workspaceFolder}/index.html"  // غيرها لاسم ملفك
        }
    ]
}